models:
  - name: 'dim__customer'
    columns:
      - name: customer_key
        tests: [not_null, unique]

      - name: customer_id
        tests: [not_null]

      - name: is_current
        tests:
          - accepted_values:
              arguments:
                values: [0, 1]
                quote: false