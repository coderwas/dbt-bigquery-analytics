models:
  - name: int__order_lines

    columns:
      - name: order_line_id
        tests:
          - unique
          - not_null

      - name: order_id
        tests: [not_null]

      - name: product_id
        tests: [not_null]

      - name: revenue
        tests: [not_null]